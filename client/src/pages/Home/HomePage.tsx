import React, {FC} from 'react';

import './HomePage.css';

const HomePage: FC = () => {
    return (
        <div className={'home_page_wrapper'}>
            HomePage
        </div>
    );
};

export default HomePage;